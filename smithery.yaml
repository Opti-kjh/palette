# Smithery.ai MCP Server Configuration
# npm 패키지 직접 사용 방식

runtime: container

startCommand:
  type: stdio
  command: npx
  args:
    - "-y"
    - "palette-mcp"
  env:
    FIGMA_ACCESS_TOKEN: ""
    GITHUB_TOKEN: ""
    FIGMA_MCP_SERVER_URL: "http://127.0.0.1:3845/mcp"
